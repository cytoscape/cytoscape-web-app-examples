{
  "name": "@cytoscape-web/network-statistics",
  "description": "Non-React example: compute and log network statistics using the Graph Traversal API",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "build": "vite build",
    "dev": "vite",
    "typecheck": "tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.node.json && tsc --noEmit -p tsconfig.test.json",
    "test": "vitest run"
  },
  "//peerDependencies": [
    "Deliberately absent. This app shares NOTHING with the host: it has no UI",
    "and imports neither React nor MUI, so there are no singletons to declare.",
    "configuredShared in ../apps.manifest.json is {} to match."
  ],
  "//devDependencies": [
    "@types/react is required even though no runtime code touches React: the",
    "published @cytoscape-web/api-types declarations reference React types, so",
    "`tsc` needs them to resolve. Removing it looks like a tidy-up and breaks",
    "typecheck."
  ],
  "devDependencies": {
    "@cytoscape-web/api-types": "^1.0.0-beta.3",
    "@module-federation/runtime": "2.5.1",
    "@module-federation/vite": "1.16.8",
    "@types/node": "^24.0.0",
    "@types/react": "^18.3.5",
    "typescript": "^5.6.2",
    "vite": "8.0.13",
    "vitest": "^4.1.8"
  }
}
