name: Release packages

# Publishes @cytoscape-web/app-runtime and create-cytoscape-app to npm.
#
# From CI rather than a workstation, for one reason above the others: the SDK is
# a Vite plugin, so it runs as CODE on every app author's build machine. A
# compromised release rewrites the output of every app built with it. That is a
# higher-value target than a runtime library, and it is why this repository —
# whose whole subject is what an installed app is trusted with — should not ship
# its own tooling from an unattested source.
#
# Provenance needs `id-token: write` and a supported CI, so a manual `npm
# publish` cannot produce it at all. Doing this once also makes it the default
# for every later release; the manual path is a cost paid again every time.
#
# Prerequisites, one-time, in repository settings:
#   1. An environment named `release`, with required reviewers. That is the
#      "protected release environment" — it is what stops a push from
#      publishing without a human.
#   2. A credential, and the FIRST release necessarily differs from the rest:
#
#      npm configures a trusted publisher on a package's SETTINGS PAGE, which
#      exists only once the package has been published. Neither of these has
#      been. So 0.1.0 goes out with a granular access token in NPM_TOKEN —
#      still from CI, still attested, still behind the protected environment —
#      and trusted publishing is configured immediately afterwards.
#
#      From 0.1.1 on: OIDC, no token, and npm generates provenance by itself
#      (the --provenance flag below becomes redundant but stays, because it is
#      what attests the token-based first publish). DELETE the two
#      NODE_AUTH_TOKEN lines, and the token, once that is set up — a credential
#      that outlives its purpose is the thing this design is trying not to have.
#
#      Runbook §3.1 has the field-by-field setup.
#   3. The repository must be public for provenance to be attestable.

on:
  workflow_dispatch:
    inputs:
      tag:
        description: 'npm dist-tag. `latest` is refused — see the gate below.'
        required: true
        default: next
      dry_run:
        description: 'Rehearse without publishing'
        type: boolean
        required: true
        default: true

concurrency:
  group: release-packages
  cancel-in-progress: false

jobs:
  release:
    name: Publish to npm
    runs-on: ubuntu-latest
    timeout-minutes: 25
    environment: release
    permissions:
      contents: read
      id-token: write # provenance
    steps:
      # Do not MOVE `latest` forward.
      #
      # Read this together with the deprecation step at the end. npm assigns
      # `latest` to a new package's first version on its own and will not let it
      # be deleted, so "no latest tag" was never achievable — that was a mistake
      # in the original design, corrected after 0.1.0 went out carrying both
      # `next` and `latest`.
      #
      # What this step still buys is real: publishing with --tag next leaves
      # `latest` pointing at whatever it already pointed at, so a later Preview
      # cannot become the default install by accident. Dispatching with
      # `tag: latest` is the one way to move it, and that stays a deliberate act
      # visible in a diff until roadmap Theme G is done.
      - name: Refuse to publish `latest`
        if: ${{ inputs.tag == 'latest' }}
        run: |
          # Single quotes: a backtick inside double quotes is command
          # substitution, and the shell would try to run `latest`.
          echo '::error::Moving the latest tag is gated on roadmap Theme G (app isolation, capability API, artifact integrity). Publish with tag=next; latest stays where it is. If Theme G has landed, remove this step in the same commit that records why.'
          exit 1

      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version-file: .nvmrc
          cache: npm
          # Required for npm to authenticate on publish.
          registry-url: https://registry.npmjs.org

      - run: npm ci

      # The same battery the runbook's pre-flight describes. It runs HERE rather
      # than being trusted to have been run: a release is exactly the moment
      # someone skips a check because they ran it an hour ago.
      - run: npm run build
      - run: npm run verify:federation
      - run: npm run typecheck --workspaces
      - run: npm test

      # Provenance is validated by the REGISTRY, against package.json's
      # repository field. `npm publish --dry-run` never contacts the registry,
      # so it cannot catch a missing or mismatched url — the 0.1.0 attempt
      # passed every local check and every dry run, then failed with a 422 after
      # burning an approval. Checking it here costs a second and fails before
      # anything is uploaded.
      - name: Check the repository field provenance will be validated against
        run: |
          set -euo pipefail
          expected="https://github.com/${{ github.repository }}"
          for dir in packages/app-runtime packages/create-cytoscape-app; do
            url=$(node -p "require('./$dir/package.json').repository?.url ?? ''")
            normalized=${url#git+}
            normalized=${normalized%.git}
            if [ "$normalized" != "$expected" ]; then
              echo "::error::$dir/package.json repository.url is '$url'," \
                   "which normalizes to '$normalized'. Provenance will reject" \
                   "anything that is not '$expected'."
              exit 1
            fi
            echo "  $dir -> $normalized"
          done

      # What is actually about to be uploaded. `files` is an allowlist, so a
      # missing entry produces a package that installs and then fails at use.
      - name: Show the tarball contents
        run: |
          npm pack -w @cytoscape-web/app-runtime --dry-run
          npm pack -w create-cytoscape-app --dry-run

      # Which versions are already on the registry. npm refuses to republish a
      # version, so a run that failed halfway could otherwise never be resumed:
      # the first package would 403 before the second one was reached. Skipping
      # is not a shortcut — it is exactly equivalent to the failure npm would
      # produce, minus the dead end.
      - name: Decide what still needs publishing
        id: todo
        run: |
          set -uo pipefail
          for w in @cytoscape-web/app-runtime create-cytoscape-app; do
            v=$(node -p "require('./packages/'+('$w'==='create-cytoscape-app'?'create-cytoscape-app':'app-runtime')+'/package.json').version")
            key=$(echo "$w" | tr -c 'a-zA-Z0-9' '_')
            if npm view "$w@$v" version --registry https://registry.npmjs.org >/dev/null 2>&1; then
              echo "  $w@$v is already published — will skip"
              echo "publish$key=false" >> "$GITHUB_OUTPUT"
            else
              echo "  $w@$v is not on the registry — will publish"
              echo "publish$key=true" >> "$GITHUB_OUTPUT"
            fi
          done

      - name: Publish @cytoscape-web/app-runtime
        if: ${{ steps.todo.outputs.publish_cytoscape_web_app_runtime == 'true' }}
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: |
          npm publish -w @cytoscape-web/app-runtime \
            --provenance --access public --tag "${{ inputs.tag }}" \
            ${{ inputs.dry_run && '--dry-run' || '' }}

      # Second, deliberately. The scaffolder does not depend on the SDK, but
      # publishing the SDK first means the first project anyone scaffolds
      # installs cleanly rather than racing the registry.
      - name: Publish create-cytoscape-app
        if: ${{ steps.todo.outputs.publish_create_cytoscape_app == 'true' }}
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: |
          npm publish -w create-cytoscape-app \
            --provenance --access public --tag "${{ inputs.tag }}" \
            ${{ inputs.dry_run && '--dry-run' || '' }}

      # Mark every published version as a Preview, and read back what the
      # registry actually has.
      #
      # This replaces a check that could not work. The original design withheld
      # the `latest` dist-tag as the release gate — but npm assigns `latest` to a
      # brand-new package's first version whatever --tag says, and the registry
      # REFUSES to delete it (400, after a successful auth; the CLI docs do not
      # mention the restriction). Every published package has a `latest`. The
      # gate as written was unachievable.
      #
      # A deprecation notice is the mechanism that does work, and is arguably the
      # stronger one: almost nobody notices a missing dist-tag, while npm prints
      # a deprecation on every single install. Combined with 0.x versioning it
      # says what the tag was meant to say, to the people who need to hear it.
      #
      # Runs whenever the release was not a dry run, failed steps or not: a
      # version published by a partially-failed run needs the notice most.
      - name: Mark the published versions as a Developer Preview
        if: ${{ !cancelled() && !inputs.dry_run }}
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: |
          set -uo pipefail
          NOTICE="Developer Preview (0.x). Cytoscape Web apps run with the host's full privileges - no sandbox, no signature verification. Not for production use: https://github.com/${{ github.repository }}"
          failed=0
          for dir in packages/app-runtime packages/create-cytoscape-app; do
            name=$(node -p "require('./$dir/package.json').name")
            version=$(node -p "require('./$dir/package.json').version")
            encoded=$(node -p "encodeURIComponent('$name')")

            if ! curl -sf "https://registry.npmjs.org/$encoded?cb=$RANDOM" -o /tmp/pkg.json; then
              echo "  $name — not on the registry, nothing to mark"
              continue
            fi

            npm deprecate "$name@$version" "$NOTICE" || { echo "::error::could not deprecate $name@$version"; failed=1; }

            curl -s "https://registry.npmjs.org/$encoded?cb=$RANDOM" -o /tmp/pkg.json
            node -e '
              const d = JSON.parse(require("fs").readFileSync("/tmp/pkg.json", "utf8"))
              const v = d.versions[process.argv[1]]
              console.log("  " + process.argv[2])
              console.log("    dist-tags:  " + JSON.stringify(d["dist-tags"]))
              console.log("    deprecated: " + (v?.deprecated ? "yes" : "NO"))
              if (!v?.deprecated) { console.log("::error::" + process.argv[2] + " carries no Preview notice"); process.exit(1) }
            ' "$version" "$name" || failed=1
          done
          exit $failed
